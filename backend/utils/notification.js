const sendNotification = async (userId, message) => {
    // This function can be used to send notifications to users.
    // Implementation may vary depending on notification service.
  };
  
  module.exports = { sendNotification };
  